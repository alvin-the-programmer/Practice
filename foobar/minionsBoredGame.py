cache = {
		(1, 2): 1,
		(3, 2): 3
}

def numberWays(t, n, cache):
	if (t, n) in cache:
		return cache[(t, n)]

def bruteForce(t, n):


def move(t, n, pos, moves):
	if pos < 1:
		return
	if pos > n:
		return


